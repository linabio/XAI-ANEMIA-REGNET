# Main configuration file
model:
  regnet:
    model_path: models/trained_models/best_regnet_anemia_classifier.pth
  unet:
    model_path: models/trained_models/best_unet_model.pth

data:
  sample_images: data/raw/sample_images

logging:
  level: INFO
  file: logs/app.log